apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-monitor-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kafka-monitor-service
            port:
              number: 9000
