apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fastapi-producer-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fastapi-producer-service
            port:
              number: 8000
